import {useEffect,useState} from 'react';
import {ArrowLeft,ArrowRight,Bell,Box,Check,ChevronRight,ClipboardCheck,Clock3,FileSearch,LayoutDashboard,MapPin,MessageCircle,PackageCheck,RotateCcw,Search,ShieldCheck,Sparkles,Truck,User,UserCheck,X} from 'lucide-react';
import './demo.css';

type Role='guest'|'management';
type Stage='reported'|'investigating'|'matched'|'return'|'reunited';
const stages:Stage[]=['reported','investigating','matched','return','reunited'];
const stageLabels={reported:'Report received',investigating:'Investigation underway',matched:'Item matched',return:'Return arranged',reunited:'Reunited'};

export function DemoApp({initialRole,close}:{initialRole:Role;close:()=>void}){
 const [role,setRole]=useState<Role>(initialRole); const [stage,setStage]=useState<Stage>('reported'); const [guestStep,setGuestStep]=useState(initialRole==='guest'?0:3); const [returnMethod,setReturnMethod]=useState<'pickup'|'authorized'|'shipping'|null>(null); const [toast,setToast]=useState('');
 useEffect(()=>{const saved=localStorage.getItem('found-again-demo');if(saved)try{const s=JSON.parse(saved);setStage(s.stage||'reported');setReturnMethod(s.returnMethod||null)}catch{}},[]);
 useEffect(()=>localStorage.setItem('found-again-demo',JSON.stringify({stage,returnMethod})),[stage,returnMethod]);
 const notify=(msg:string)=>{setToast(msg);setTimeout(()=>setToast(''),2600)};
 const advance=(next:Stage,msg:string)=>{setStage(next);notify(msg)};
 const reset=()=>{setStage('reported');setReturnMethod(null);setGuestStep(0);localStorage.removeItem('found-again-demo');notify('Demo reset to the beginning')};
 return <div className="demoApp">
  <header className="demoBar"><button className="demoBrand" onClick={close}><span><Sparkles/></span><b>Found Again</b><small>Live product demo</small></button><div className="roleSwitch"><button className={role==='guest'?'active':''} onClick={()=>setRole('guest')}><User/> Guest experience</button><button className={role==='management'?'active':''} onClick={()=>setRole('management')}><LayoutDashboard/> Resort management</button></div><div className="demoTools"><button onClick={reset} title="Reset demo"><RotateCcw/></button><button onClick={close}><X/><span>Exit demo</span></button></div></header>
  <div className="demoNotice"><Sparkles/> Connected demo · Actions in management instantly update the guest view <button onClick={()=>setRole(role==='guest'?'management':'guest')}>Switch perspective <ArrowRight/></button></div>
  {role==='guest'?<GuestView stage={stage} guestStep={guestStep} setGuestStep={setGuestStep} returnMethod={returnMethod} chooseReturn={(m)=>{setReturnMethod(m);advance('return',m==='shipping'?'Shipping selected—management has been notified':'Pickup selected—management has been notified')}}/>:<ManagementView stage={stage} returnMethod={returnMethod} advance={advance}/>} 
  {toast&&<div className="demoToast"><Check/> {toast}</div>}
 </div>
}

function GuestView({stage,guestStep,setGuestStep,returnMethod,chooseReturn}:{stage:Stage;guestStep:number;setGuestStep:(n:number)=>void;returnMethod:string|null;chooseReturn:(m:'pickup'|'authorized'|'shipping')=>void}){
 const submitted=guestStep>=3;
 return <div className="guestDemo"><aside className="guestAside"><div className="sampleTag">Sample resort</div><h1>Lost something?<br/><em>Let’s find it.</em></h1><p>Tell us what happened. You won’t need an account, and your resort team will keep you updated.</p><div className="guestHelp"><MessageCircle/><span><b>Need help?</b><small>A resort team member can assist with your report.</small></span></div></aside><section className="guestWorkspace">
   {!submitted?<><div className="wizardHead"><span>Lost item report</span><small>About 2 minutes</small></div><div className="wizardSteps">{['Where & when','Your item','Your details'].map((x,i)=><span className={i<=guestStep?'active':''} key={x}><i>{i<guestStep?<Check/>:i+1}</i>{x}</span>)}</div>{guestStep===0&&<div className="formCard"><p className="mini">Step 1 of 3</p><h2>Where did you last have it?</h2><label>Resort property</label><button className="select">Cascade Bay Resort & Casino <ChevronRight/></button><div className="twoCol"><div><label>Area</label><button className="select"><MapPin/> North Tower</button></div><div><label>Approximate time</label><button className="select"><Clock3/> Today · 9:30 AM</button></div></div><button className="demoPrimary" onClick={()=>setGuestStep(1)}>Continue <ArrowRight/></button></div>}{guestStep===1&&<div className="formCard"><p className="mini">Step 2 of 3</p><h2>What should we look for?</h2><div className="itemPreview"><span>🕶️</span><div><b>Round black sunglasses</b><small>Tortoiseshell arms · gold initials “ML” inside</small></div><Check/></div><label>Where exactly?</label><div className="demoInput">Room 1428, probably on the nightstand</div><label>Private ownership detail</label><div className="demoInput secure"><ShieldCheck/> Tiny scratch on the left lens</div><button className="demoPrimary" onClick={()=>setGuestStep(2)}>Continue <ArrowRight/></button></div>}{guestStep===2&&<div className="formCard"><p className="mini">Step 3 of 3</p><h2>Where should we send updates?</h2><label>Name</label><div className="demoInput">Maria Lopez</div><label>Email & mobile</label><div className="twoCol"><div className="demoInput">maria@example.com</div><div className="demoInput">(555) 014-2882</div></div><div className="consent"><Check/> Send private report updates by email and text</div><button className="demoPrimary" onClick={()=>setGuestStep(3)}>Submit sample report <ArrowRight/></button></div>}</>:<GuestTracker stage={stage} returnMethod={returnMethod} chooseReturn={chooseReturn}/>} 
  </section></div>
}

function GuestTracker({stage,returnMethod,chooseReturn}:{stage:Stage;returnMethod:string|null;chooseReturn:(m:'pickup'|'authorized'|'shipping')=>void}){const current=stages.indexOf(stage);return <div className="tracker"><div className="trackerHead"><div><p className="mini">Private report FA-84219</p><h2>{stage==='reunited'?'Welcome back, sunglasses.':'We’re on it, Maria.'}</h2><p>Round black sunglasses · North Tower</p></div><span className={'statusPill '+stage}>{stageLabels[stage]}</span></div><div className="guestTimeline">{stages.map((s,i)=><div key={s} className={i<=current?'done':''}><i>{i<current?<Check/>:i+1}</i><span><b>{stageLabels[s]}</b><small>{i===0?'Report received today at 10:06 AM':i===1?'Security is checking North Tower inventory':i===2?'Ownership details privately verified':i===3?(returnMethod?`${returnMethod==='shipping'?'Tracked shipping':'Verified pickup'} selected`:'Choose how you want it back'):'Return completed with custody receipt'}</small></span></div>)}</div>{stage==='matched'&&<div className="returnChoice"><p className="mini">Good news—it’s yours</p><h3>How would you like it back?</h3><div>{[['pickup','Verified pickup','Free · Ready today'],['authorized','Authorized person','Free · Secure handoff'],['shipping','Tracked shipping','$18.40 · Arrives Tue']].map(([m,a,b])=><button key={m} onClick={()=>chooseReturn(m as 'pickup'|'authorized'|'shipping')}><span>{m==='shipping'?<Truck/>:<UserCheck/>}<b>{a}</b><small>{b}</small></span><ChevronRight/></button>)}</div></div>}{stage==='return'&&<div className="guestMessage"><Bell/><div><b>Your return is being prepared</b><p>{returnMethod==='shipping'?'UPS tracking 1Z84•••219 will activate after the package leaves the resort.':'Bring your secure pickup QR and photo ID to Resort Security.'}</p></div></div>}{stage==='reunited'&&<div className="celebrate"><Sparkles/><div><b>Case closed—with a happy ending.</b><p>A complete release receipt is available for this return.</p></div></div>}</div>}

function ManagementView({stage,returnMethod,advance}:{stage:Stage;returnMethod:string|null;advance:(s:Stage,m:string)=>void}){const [view,setView]=useState<'queue'|'case'>('queue');return <div className="management"><aside className="sideNav"><div><p>CASCADE BAY</p><b>Resort Operations</b></div>{[['queue',LayoutDashboard,'Command center'],['case',FileSearch,'Cases'],['',Box,'Found inventory'],['',ClipboardCheck,'Custody log']].map(([id,Icon,label])=><button key={label as string} className={view===id?'active':''} onClick={()=>id&&setView(id as 'queue'|'case')}><Icon/> {label as string}{label==='Command center'&&<i>3</i>}</button>)}<div className="sideBottom"><ShieldCheck/><span><b>Security Manager</b><small>Full property access</small></span></div></aside><main className="managementMain"><div className="managementTop"><div><p className="mini">Thursday · August 20</p><h1>{view==='queue'?'Command center':'Case FA-84219'}</h1></div><div className="search"><Search/> Search cases & inventory</div><button className="avatar">AD</button></div>{view==='queue'?<Queue stage={stage} open={()=>setView('case')}/>:<CaseDetail stage={stage} returnMethod={returnMethod} advance={advance}/>}</main></div>}

function Queue({stage,open}:{stage:Stage;open:()=>void}){return <><div className="metrics">{[['New inquiries','3','Needs review'],['Suggested matches',stage==='reported'?'1':'0','Ready to compare'],['Awaiting return',stage==='return'?'1':'4','Pickup or shipping'],['Reunited today',stage==='reunited'?'13':'12','Completed cases']].map(([a,b,c],i)=><article key={a} className={i===1?'highlight':''}><span>{a}</span><strong>{b}</strong><small>{c}</small></article>)}</div><div className="queuePanel"><div className="panelHead"><div><h2>Needs attention</h2><p>Prioritized across all resort departments</p></div><button>View all <ArrowRight/></button></div><button className="caseRow featured" onClick={open}><span className="caseIcon">🕶️</span><span><b>Round black sunglasses</b><small>FA-84219 · Maria Lopez · North Tower</small></span><span className="matchScore"><small>Suggested match</small><b>{stage==='reported'?'92%':stageLabels[stage]}</b></span><span className="owner"><i>SEC</i><small>Security</small></span><ChevronRight/></button>{[['📱','Black smartphone','FA-84197 · Grand Ballroom','New inquiry'],['👜','Coral evening bag','FI-20831 · Luna Restaurant','Awaiting owner']].map(x=><button className="caseRow" key={x[1]}><span className="caseIcon">{x[0]}</span><span><b>{x[1]}</b><small>{x[2]}</small></span><span className="simpleStatus">{x[3]}</span><span className="owner"><i>—</i><small>Unassigned</small></span><ChevronRight/></button>)}</div></>}

function CaseDetail({stage,returnMethod,advance}:{stage:Stage;returnMethod:string|null;advance:(s:Stage,m:string)=>void}){return <div className="caseDetail"><div className="caseHero"><button className="backLink"><ArrowLeft/> All cases</button><div className="caseTitle"><span>🕶️</span><div><p className="mini">Lost-item inquiry · FA-84219</p><h2>Round black sunglasses</h2><p>Reported by Maria Lopez · Today at 10:06 AM</p></div><span className={'statusPill '+stage}>{stageLabels[stage]}</span></div></div><div className="caseColumns"><div><section className="compare"><div className="sectionTitle"><div><p className="mini">Suggested match</p><h3>Found item FI-20847</h3></div><span className="confidence">92% match</span></div><div className="compareGrid"><div><p className="mini">Guest report</p><h4>Black round sunglasses</h4><span><MapPin/> North Tower · Room 1428</span><span><Clock3/> Lost around 9:30 AM</span><span><ShieldCheck/> Private clue: scratch on left lens</span></div><div><p className="mini">Found inventory</p><h4>Round sunglasses, tortoise arms</h4><span><MapPin/> Room 1428 · Nightstand</span><span><Clock3/> Found at 10:42 AM</span><span><Box/> Vault B · Bin 17</span></div></div><div className="reasons"><span>Exact room</span><span>Time proximity</span><span>Color & shape</span><span>Gold “ML” initials</span></div>{stage==='reported'&&<button className="mgmtPrimary" onClick={()=>advance('investigating','Investigation started—guest status updated')}>Begin investigation <ArrowRight/></button>}{stage==='investigating'&&<button className="mgmtPrimary" onClick={()=>advance('matched','Match approved—Maria has been notified')}>Verify ownership & approve match <Check/></button>}{stage==='matched'&&<p className="waiting"><Clock3/> Waiting for Maria to choose pickup or shipping. Switch to Guest Experience to continue.</p>}{stage==='return'&&<button className="mgmtPrimary" onClick={()=>advance('reunited','Custody released—case marked reunited')}>Record {returnMethod==='shipping'?'carrier handoff':'verified pickup'} <PackageCheck/></button>}{stage==='reunited'&&<p className="complete"><Check/> Return completed and custody receipt recorded.</p>}</section><section className="activity"><div className="sectionTitle"><h3>Case activity</h3><button>Internal notes</button></div>{[['10:42 AM','Jordan K. · EVS','Found item logged from Room 1428'],['10:43 AM','Found Again','92% match suggested automatically'],['10:46 AM','Alex D. · Security',stage==='reported'?'Awaiting review':stage==='investigating'?'Investigation opened':'Ownership detail verified'],['Now','Guest notification',stage==='matched'?'Match confirmation sent':stage==='return'?'Return choice received':stage==='reunited'?'Case-closed receipt sent':'Report confirmation delivered']].map(x=><div className="event" key={x[0]+x[2]}><i/><small>{x[0]}</small><span><b>{x[1]}</b>{x[2]}</span></div>)}</section></div><aside className="caseSide"><section><p className="mini">Assigned team</p><div className="assignee"><i>AD</i><span><b>Alex Diaz</b><small>Security Manager</small></span></div></section><section><p className="mini">Chain of custody</p>{[['10:42','Found','Jordan K. · EVS'],['10:49','Secured','Vault B · Bin 17'],[stage==='reunited'?'Now':'—',stage==='reunited'?'Released':'Next handoff',stage==='reunited'?'Receipt signed':'Pending return']].map(x=><div className="custody" key={x[1]}><Check/><span><b>{x[1]}</b><small>{x[0]} · {x[2]}</small></span></div>)}</section><section><p className="mini">Guest contact</p><b>Maria Lopez</b><small>maria@example.com<br/>(555) 014-2882</small><button><MessageCircle/> Send update</button></section></aside></div></div>}
